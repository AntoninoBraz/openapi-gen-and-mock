import { Routes } from '@angular/router';
import { ApiUsageDemoComponent } from './features/api-usage-demo/api-usage-demo.component';

export const routes: Routes = [{ path: '', component: ApiUsageDemoComponent }];
